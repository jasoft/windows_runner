# Agents
用 nodejs框架和合适的 api 框架,实现一个 API，可以在 host 这个 API 的 Windows 机器上执行指定的命令，包括 GUI 命令。就是我想从另外一台机器访问 API，直接在 Windows 叉调用程序，比如说计算器 Notepad 之类的都可以直接调用。因为 SSH 无法启动 GUI 程序，所以做不到这一点。如果可能的话，做成一个 Docker 程序.

API 用 swagger生成详细的文档.

你需要先把要做的事情生成详细的 task 然后再动手.

跟我用中文交流.
